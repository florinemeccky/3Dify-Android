<?xml version="1.0" encoding="utf-8"?>
<!--
    Redesigned Profile Menu Card
    Height: 74dp
    Card-style with 12dp radius and 1dp border.
    Internal padding: 18dp
    Vertical spacing: 12dp (via margin)
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="74dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="18dp"
    android:paddingEnd="18dp"
    android:layout_marginBottom="12dp"
    android:layout_marginStart="20dp"
    android:layout_marginEnd="20dp"
    android:background="@drawable/bg_profile_card"
    android:clickable="true"
    android:focusable="true">

    <!-- Icon — 24dp fixed size -->
    <ImageView
        android:id="@+id/iv_row_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:scaleType="fitCenter"
        android:alpha="0.7"
        android:layout_marginEnd="20dp"/>

    <!-- Label — 16sp title -->
    <TextView
        android:id="@+id/tv_row_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Menu Item"
        android:textColor="@color/text_primary"
        android:textSize="16sp"
        android:textStyle="bold"/>

    <!-- Arrow — subtle indicator -->
    <ImageView
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:src="@drawable/ic_arrow_right"
        android:scaleType="fitCenter"
        android:alpha="0.25"/>

</LinearLayout>