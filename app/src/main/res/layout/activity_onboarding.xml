<?xml version="1.0" encoding="utf-8"?>
<!--
    ONBOARDING ACTIVITY
    ════════════════════
    WHY: First-run experience. Explains the app's value before signup.
    FLOW: Splash → Onboarding (first run only) → LoginActivity
    3 swipeable pages via ViewPager2.
    Skip button jumps directly to Login.
    Get Started on page 3 also goes to Login.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/colorBackground">

    <!-- ── Skip button — top right ── -->
    <TextView
        android:id="@+id/tv_skip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_skip"
        android:textColor="@color/gray_text"
        android:textSize="13sp"
        android:padding="16dp"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ── ViewPager2 fills the middle section ── -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/vp_onboarding"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/tv_skip"
        app:layout_constraintBottom_toTopOf="@id/btn_get_started"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ── Primary CTA button ── -->
    <Button
        android:id="@+id/btn_get_started"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="12dp"
        android:text="@string/btn_get_started"
        android:textColor="@color/black_primary"
        android:textStyle="bold"
        android:textSize="14sp"
        android:background="@drawable/bg_btn_orange"
        app:layout_constraintBottom_toTopOf="@id/ll_dots"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ── Page indicator dots ── -->
    <LinearLayout
        android:id="@+id/ll_dots"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="32dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <View android:id="@+id/dot1"
            android:layout_width="18dp" android:layout_height="4dp"
            android:layout_marginEnd="6dp"
            android:background="@drawable/bg_btn_orange"/>
        <View android:id="@+id/dot2"
            android:layout_width="6dp" android:layout_height="4dp"
            android:layout_marginEnd="6dp"
            android:background="@color/border_dark"/>
        <View android:id="@+id/dot3"
            android:layout_width="6dp" android:layout_height="4dp"
            android:background="@color/border_dark"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>